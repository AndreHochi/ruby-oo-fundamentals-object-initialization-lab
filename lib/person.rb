#Generate Person object.
#Person object should initialize with a name.
#Person attributes should have a getter and a setter.

class Person

    def initialize(name)
        @name = name
    end

    def name=(name)
        @name = name
    end

    def name
        @name
    end
end
